# Open Questions — circle

## 1. 实体关系升级：建不建类型化关系表？（P3 最大项）

现状：边只有「共现」weight（同原子同时关联两实体的次数），无类型/方向/语义。
「张三 ∈ 后端组」「上海 = 长亭科技所在地」表达不了。

- 拍板点 A：是否建 `entity_relations` 表（from/to/type），还是维持「共现 + 关系摘要写在画像里」的 MVP 形态？
- 拍板点 B：关系类型枚举（成员/地点/项目/…）谁来定、谁写（蒸馏抽取 vs 手动建）？
- 历史约束：e2174fda9b28 曾拍板「关系语义写摘要、不建类型化关系表」——本次用户松口「可引全新能力」，是否推翻该取舍？

## 2. 时间轴视图的形态

「按时间浏览实体/记忆」是实体时间线，还是全局记忆时间轴？与 /memory 的会话/原子 tab 如何不重复？

## 3. 批量操作的破坏性边界

批量 merge/delete/归档会放大误操作半径。是否需要独立 scope 或二次确认？参考 deep purge 的两阶段 arm/token 模式。

## 4. 数据密度瓶颈的应对

圈子图的价值依赖数据密度（当前 16 实体全 atom_count=1）。是「等数据自然积累」，还是「降级为列表为主、图为辅」过渡？见 topics/data-density.md。
