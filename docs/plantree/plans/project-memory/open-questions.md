# Open Questions

设计对齐推进后的剩余未决（2026-09-04 第二轮后）：

1. **文档与 knowledge 域的关系**：project_docs 自带 markdown 内容已定，但要不要打通 knowledge 的检索管线（文档可被全文检索）？还是项目文档检索走独立的 LIKE/FTS？
2. **轻结构字段集**：md + 轻结构已定，但元数据具体有哪些（状态标签/负责人/顺序/置顶）？状态枚举值？
3. **AI 检测导入的形态**：「拿到项目 → 检测本地文档 → 登记」具体是 memory.py 什么命令？扫描哪些约定目录（docs/、server/docs/…）？增量同步还是一次性导入？
4. **project slug 与主机路径的绑定关系**：同一 repo 在多主机（Mac 开发 + 服务器部署）是两个 location 还是两个 project？倾向前者（location.purpose 区分用途），待实施时验证。
5. **删除语义**：项目删除是否级联删 locations/docs？软删（归档）还是硬删？与现有 deep purge/erase scope 的权限关系？
6. **context pack 的接口形态**：AI 开工拉上下文——专用端点（一次拉位置+规划+指定分类最新 N 篇）还是客户端多次调用？收工沉淀（文档更新）走 PUT 即可还是有专门协议？
7. **调研类型分类预设确认**：「待查/线索/资料/结论/疑点/证伪」六分类是对齐时的草案，调研类型实施前需用户确认。

已决问题移入 [decisions/](decisions/)（挂靠方式→0005 三表；骨架表达→0005 类型=分类模板；plan-tree 形态→0004；录入入口→0005 双入口）。
