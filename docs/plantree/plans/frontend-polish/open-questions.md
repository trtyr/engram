# Open Questions

1. ~~**/jobs 硬刷新返回 JSON**~~ → 已解决（2026-08-30，决策 D-001：后端 Accept 分流已落地）。
2. **toast/操作反馈系统**要不要引入（现在保存/删除只有内联小字反馈）——Engram 极简下用轻量 toast 还是保持内联？倾向：保持内联，暂不引入。
3. ~~侧栏状态徽章的轮询间隔~~ → 已定：10s（页面不可见跳过，2026-08-30 落地）。
